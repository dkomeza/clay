import { Modal, ModalContent, ModalHeader, ModalBody } from "@nextui-org/react";

function OpenSpace() {
  return (
    <Modal>
      <ModalContent>
        <ModalHeader>Open Space</ModalHeader>
      </ModalContent>
    </Modal>
  );
}

export default OpenSpace;
